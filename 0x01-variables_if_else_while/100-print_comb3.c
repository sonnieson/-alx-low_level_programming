#include<stdio.h>
/**
 * main - prints all possible different combinations of two digits
 *
 * Return: 0 (Success)
 */
int main(void)
{
int n, m;
for (n = 1; n < 10; n++)
{
for (m = i + 1; m < 10 ; m++)
{
putchar(n + '0');
putchar(m + '0');
putchar(',');
putchar(' ');
putchar(' ');
}
}
putchar('\n');
return (0); }
